spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    url: jdbc:h2:mem:poc
    password: ''
  h2:
    console:
      enabled: 'true'
      settings:
        trace: 'false'
      path: /console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

